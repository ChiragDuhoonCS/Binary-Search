#include<bits/stdc++.h>
using namespace std;

void brute(int n,int arr[], int d){
    for (int i = arr[0]; i <= arr[n-1]; i++)
    {
        
    }
    
}

int main() {
    int arr[] = {1,2,5,9};
    int n = sizeof(arr) / sizeof(arr[0]);
    int d = 6;

    printf("Your peak value is at: %d ",arr);

    return 0;
}